# envs/stage/.env.template
# Per-environment overrides for the `stage` deployment. Merged over the
# root .env.template by install.sh/update.sh.

BUILD_ENV=stage
DOMAIN=stage.example.com

PROJECT_DISPLAY_NAME="Apostol CSMS (stage)"
PROJECT_URL=https://cloud.stage.example.com
PROJECT_SCOPE_ALIAS=https://cloud.stage.example.com
