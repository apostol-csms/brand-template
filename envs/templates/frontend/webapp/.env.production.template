# Здесь живёт ровно одна переменная, и это не недосмотр.
#
# Брендовые значения (client_id и scope OAuth2, имя проекта, ключи карт,
# publishable-ключи платёжных провайдеров) с переработки лицензии 28.04.2026
# приходят из ПОДПИСАННОГО манифеста: GET /api/v1/manifest, подпись проверяется
# фронтом через вшитый platform-pub.pem. В коде это useBrand() / brand.oauth.
# Править их надо в db.brand_manifest (PATCH /api/v1/manifest, scope admin),
# а не здесь. Пять строк NEXT_PUBLIC_* убраны отсюда 27.08.2026 именно поэтому:
# они не читались ни кодом, ни сборкой, но выглядели как настройка.
#
# Второе, что стоит знать, прежде чем сюда что-то дописывать: отрендеренный
# файл workdir/frontend/webapp/.env.production в контейнер НЕ ПОПАДАЕТ. Ни один
# из четырёх SPA-сервисов его не монтирует — все берут env_file: workdir/.env
# (проверено 31.08.2026 по compose шаблона и четырёх брендов). Сборка webapp
# вшивает свои три ARG из Dockerfile. Так что новая строка здесь не доедет
# никуда, пока кто-то не заведёт ей доставку.
NEXT_PUBLIC_API_HOST=${NEXT_PUBLIC_API_HOST}
